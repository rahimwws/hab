import React from "react";
import { View } from "react-native";
import { Habit } from "../../habit/model/types";

const HabitChart = ({ habits }: { habits: Habit[] }) => {
  return <View style={{ flex: 1, height: 500 }}></View>;
};

export default HabitChart;
