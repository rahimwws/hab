import React from "react";
import { ScreenContent } from "@/shared/ui/ScreenContent";
import { Typography } from "@/shared/ui/Typography";
const Journey = () => {
  return (
    <ScreenContent>
      <Typography align="left" size={22} font="p-sb">
        Journey
      </Typography>
    </ScreenContent>
  );
};

export default Journey;
